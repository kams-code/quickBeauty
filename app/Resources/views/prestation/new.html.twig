{% extends 'base.html.twig' %}

{% block body %}
    <h1>Prestation creation</h1>

    {{ form_start(form,{'attr':{'class':'form'}}) }}
        {{ form_widget(form,{'attr':{'class':'form-group'}}) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('prestation_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
